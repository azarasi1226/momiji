syntax = "proto3";

package momiji.stock;

// 在庫調整の理由（販売以外の増減）。
enum StockAdjustmentReason {
  STOCK_ADJUSTMENT_REASON_UNSPECIFIED = 0;
  STOCK_ADJUSTMENT_REASON_DAMAGED = 1;
  STOCK_ADJUSTMENT_REASON_LOST = 2;
  STOCK_ADJUSTMENT_REASON_STOCKTAKING = 3;
  STOCK_ADJUSTMENT_REASON_CORRECTION = 4;
  STOCK_ADJUSTMENT_REASON_OTHER = 5;
}
